import { MdpValidatorDirective } from './mdp-validator.directive';

describe('MdpValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MdpValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
